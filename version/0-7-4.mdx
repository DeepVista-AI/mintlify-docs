---
title: "Version 0.7.4"
description: "Set up a new project in two steps, rename a saved version from its history menu, customize your database in any workbench, press ⌘S to name a version, co-edit a note with your DeepVista agent safely, check a workflow's schedule at a glance, and trust what a backgrounded chat tab tells you."
---

September 9, 2026

#### **1. Set up a new project in two simple steps**

Creating a project now takes two simple steps instead of one long form. First, name your workbench using ready-made presets instead of a blank field. Then DeepVista opens that workbench and fills in a starting prompt for you automatically. Adding a workbench from the right-hand menu also shows its default database right away.

![Choosing a preset and starting prompt while setting up a new project](/images/release-0-7-4-two-step-project-setup.gif)

#### **2. Rename a saved version**

Open a card's ⋯ menu, hover **Versions**, and click the pen button on any saved version to give it a new name. The name belongs to that version alone, so renaming it does not change what restoring it puts back.

![Renaming a saved version from the pen button in its history list](/images/release-0-7-4-rename-version-inline.gif)

#### **3. Customize your database in any workbench**

Every workbench keeps one combined database of everything inside it, including nested workbenches. Add or move a database in and it's included right away, no setup needed. You can customize that combined database's own columns, sort order, and views just like any other database.

![Adding a database to a workbench and seeing it join the combined view](/images/release-0-7-4-workbench-combined-database.gif)

#### **4. Press ⌘S to name your current version**

On a saved, editable card, ⌘S (Ctrl+S) with your cursor in the editor now names your current version instead of falling through to the browser's save dialog. It shows up in the **?** shortcut help under the card's own shortcuts. On a card you haven't saved yet, ⌘S still saves a draft.

![Pressing Cmd+S in a card editor to name the current version](/images/release-0-7-4-cmd-s-name-version.gif)

#### **5. Co-edit a note with your DeepVista agent safely**

If you edit a card while an agent is writing to it, the agent no longer overwrites your edit with the copy it read at the start of its turn. Its write is refused as soon as the card is seen to have moved, and the agent is told to re-read your latest version, redo its change on top of it, and write again. A card nobody else is touching still saves in a single step.

![Editing a card while an agent writes to it without losing either change](/images/release-0-7-4-concurrent-edit-protection.gif)

#### **6. Check a workflow's schedule and next run at a glance**

A workflow that runs on a schedule now carries a calendar badge on its card in your dashboard feed and pinned lists, and on the Schedule button in the workflow itself. Hover the badge for the cadence and the next run time, so you don't have to open the schedule to check. A paused schedule shows the badge in amber; its next run time reappears when you re-enable it.

![Hovering a workflow card to see its schedule status and next run time](/images/release-0-7-4-workflow-schedule-status.gif)

#### **7. Trust what a backgrounded chat tab tells you**

If you background a chat tab while the agent is still working, the turn no longer looks finished before it is. It stays marked as generating, tool-call steps keep appearing without a reload, and the reply's action buttons show up only once the run actually completes.
